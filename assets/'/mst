struct road{ int x,y,z; } rd[N];
bool cmp_rd(const road&a,const road&b){ return a.z < b.z; }
void MST(int n,int m){
	sort(rd+1,rd+m+1,cmp_rd);
	int cnt = 0;
	uni.init(n);
	rep(i,1,m){
		int x=rd[i].x, y=rd[i].y;
		if( uni.merge(x,y)==0 ) continue;
		if( (++cnt) == n-1 ) break;
	}
}
