int pre[N],suf[N],vis[N],tim_dfs;
bool dfs(int u){
	each(i,u){
		int v= bow[i].to;
		if( vis[v]==tim_dfs ) continue;
		vis[v]= tim_dfs;
		if( pre[v]==0 || dfs(pre[v]) ){
			pre[v]= u;
			suf[u]= v;
			return 1;
		}
	}
	return 0;
}
bool match(int u){
	tim_dfs++;
	return dfs(u);
}
