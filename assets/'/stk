int ml[N],mr[N];
void STK(int n,int*a){
	static int stk[N];
	int top = 0;
	a[0] = a[++n] = 1e9;
	rep(i,1,n){
		while( top && a[stk[top]]<a[i] ) mr[stk[top--]] = i;
		ml[i] = stk[top];
		stk[++top] = i;
	}
}
